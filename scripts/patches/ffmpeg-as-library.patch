--- a/fftools/ffmpeg.c
+++ b/fftools/ffmpeg.c
@@ -1,3 +1,6 @@
+// Patched by react-native-nitro-ffmpeg to expose as library function
+#include "ffmpeg_kit_bridge.h"
+
 /*
  * Copyright (c) 2000-2003 Fabrice Bellard
  *
@@ -main_return:
-    exit_program(received_nb_signals ? 255 : main_return_code);
-    return main_return_code;
+    // Instead of exit(), return the code for library use
+    ffmpegkit_return_code = received_nb_signals ? 255 : main_return_code;
+    return ffmpegkit_return_code;
 }

+// Rename main to ffmpeg_execute_main for library use
+int ffmpeg_execute_main(int argc, char **argv) {
+    return main(argc, argv);
+}
--- a/fftools/cmdutils.c
+++ b/fftools/cmdutils.c
@@ -exit_program)
-    exit(ret);
+    // For library use: longjmp instead of exit
+    if (ffmpegkit_longjmp_active) {
+        longjmp(ffmpegkit_longjmp_buf, ret);
+    }
+    exit(ret);
--- /dev/null
+++ b/fftools/ffmpeg_kit_bridge.h
@@ -0,0 +1,18 @@
+#ifndef FFMPEG_KIT_BRIDGE_H
+#define FFMPEG_KIT_BRIDGE_H
+
+#include <setjmp.h>
+
+// Global state for library mode
+extern __thread int ffmpegkit_return_code;
+extern __thread int ffmpegkit_longjmp_active;
+extern __thread jmp_buf ffmpegkit_longjmp_buf;
+
+// Progress callback for library consumers
+typedef void (*ffmpegkit_progress_fn)(int64_t frame, double fps, double bitrate,
+                                      int64_t total_size, int64_t time_ms, double speed,
+                                      void *user_data);
+extern __thread ffmpegkit_progress_fn ffmpegkit_progress_callback;
+extern __thread void *ffmpegkit_progress_user_data;
+
+#endif // FFMPEG_KIT_BRIDGE_H
