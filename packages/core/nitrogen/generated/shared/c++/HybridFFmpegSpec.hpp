///
/// HybridFFmpegSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeProgress` to properly resolve imports.
namespace margelo::nitro::ffmpeg { struct NativeProgress; }
// Forward declaration of `NativeLogEntry` to properly resolve imports.
namespace margelo::nitro::ffmpeg { struct NativeLogEntry; }
// Forward declaration of `NativeSessionResult` to properly resolve imports.
namespace margelo::nitro::ffmpeg { struct NativeSessionResult; }

#include <string>
#include "NativeProgress.hpp"
#include <functional>
#include "NativeLogEntry.hpp"
#include "NativeSessionResult.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::ffmpeg {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `FFmpeg`
   * Inherit this class to create instances of `HybridFFmpegSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridFFmpeg: public HybridFFmpegSpec {
   * public:
   *   HybridFFmpeg(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridFFmpegSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridFFmpegSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridFFmpegSpec() override = default;

    public:
      // Properties
      virtual std::function<void(const std::string& /* sessionId */, const NativeProgress& /* progress */)> getOnProgress() = 0;
      virtual void setOnProgress(const std::function<void(const std::string& /* sessionId */, const NativeProgress& /* progress */)>& onProgress) = 0;
      virtual std::function<void(const std::string& /* sessionId */, const NativeLogEntry& /* log */)> getOnLog() = 0;
      virtual void setOnLog(const std::function<void(const std::string& /* sessionId */, const NativeLogEntry& /* log */)>& onLog) = 0;
      virtual std::function<void(const std::string& /* sessionId */, const NativeSessionResult& /* result */)> getOnComplete() = 0;
      virtual void setOnComplete(const std::function<void(const std::string& /* sessionId */, const NativeSessionResult& /* result */)>& onComplete) = 0;

    public:
      // Methods
      virtual void execute(const std::string& sessionId, const std::vector<std::string>& args, double logLevel) = 0;
      virtual void cancel(const std::string& sessionId) = 0;
      virtual void cancelAll() = 0;
      virtual std::vector<std::string> getActiveSessions() = 0;
      virtual std::shared_ptr<Promise<std::string>> probe(const std::string& path) = 0;
      virtual std::string getFFmpegVersion() = 0;
      virtual std::vector<std::string> getSupportedEncoders() = 0;
      virtual std::vector<std::string> getSupportedDecoders() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "FFmpeg";
  };

} // namespace margelo::nitro::ffmpeg
